/*
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MagnifierRotation : MonoBehaviour
{
    public Camera magnifierLens;
    //public Vector3;

    // Update is called once per frame
    void Update()
    {
        GameObject.transform.GetPositionAndRotation(out currentRotation);
        //Vector3 newRotation = new Vector3(currentRotation.get, currentRotation.get, 0.0f);
        //GameObject.transform.Rotate(,Space.Self);
        // z-axis rotation = 0
    }
}
*/